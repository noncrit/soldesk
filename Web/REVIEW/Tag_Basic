//Mark-up Language : 태그(Tag)를 활용해 문법을 표기하는 언어//
<!-- 
	주석은 이렇게
	버전이 명시가 따로 되어있지 않은 상태면 대부분 'HTML 5'버전
 -->

<html> </html>
	시작태그와 종료 태그가 한 쌍
	문서의 모든 내용을 감싸는 역할 
	문서의 시작과 끝을 담당함
	루트 태그(Root tag)라고 부름
	<html>의 하위 태그
		<head> : 설정, 불러올 파일 명시
		<body> : 실제 표시할 페이지 내용

<meta charset="UTF-8">
<!--  문자의 인코딩 방식, 문서 키워드, 요약 정보 등등을 담아낼 수 있는 meta 태그 -->

  <!-- 
    <body> 태그 : 브라우저에 출력할 내용을 담은 태그
    이 태그 안쪽 내용은 화면에 출력이 된다.
    텍스트, 이미지, 동영상, 음악 등등
  -->

<!--
	<br> : 단순한 줄바꿈
	<hr> : 줄을 바꾸고 나서 선을 한줄 그어줌
	<p>	 : 두 줄 바꾸기(일반적으로 문단을 나눌 때 사용함), 줄바꾸는 용도로 사용 할 경우 시작 태그만 써도 무방 	
-->

<!--
  	<div>  : 의미는 X, 어떤 목적에 따라 컨텐츠를 묶을 때 사용</div>
	<span> : 의미는 X, 어떤 목적에 따라 컨텐츠를 묶을 때 사용</span>
		div는 한 줄 전체 영역을 차지함, 줄바꿈 기능 O, 
		span은 텍스트가 쓰여있는 영역만 점유함, 줄바꿈 기능 X
-->

<!--
	 우리가 일반적으로 사용하는 스페이스바는 한번만 먹힘
	 여러 칸 띄우고 싶으면 &nbsp;
	ex)띄어&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;쓰기
-->

<!--
	부등호 표기
	가끔 부등호를 태그의 시작 부분으로 인식하는 경우가 있음 
	그래서 a&lt;X (a<X) , b&gt;Y (b>Y) 이런식으로 표기 가능
-->

<!-- <ul> / <ol>  -->
<!--
	<ul> : unordered list, 순서가 없는 리스트
	<ol> : ordered list 순서가 있는 리스트, 기호가 아닌 숫자나 영문 순서대로 삽입
		<ol 속성 = "속성값">
		속성 : type을 입력으로 받음
			type :
				1 : 숫자(기본값)
				a : 영어 소문자
				A :	영어 대문자
				i :	로마자 소문
				I :	로마자 대문
	ex)
	<ul type="1">
		<li>ddddd
		<li>dddda
	</ul> //Unordered list에 타입 붙여도 별 변화는 없음

	<ol type="A">
		<li>ddddd
		<li>dddda
	</ol> //순서 표기가 영어 대문자 기준으로 나오는 Ordered list

	<ol type="I">
		<li>ddddd
		<li>dddda
	</ol> //순서 표기가 로마자 대문자 기준으로 나오는 Ordered list
-->

<!-- 글자 관련 태그(추후에 추가 필요함)
ex)
<font size ="7" face="궁서" color="#3DB7CC">
	ㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋㅋ
</font>

<!-- 간이 태그 --> 
<b>굵게</b>
<strong>강조</strong>
<i>itatlic,기울임</i>
<em>emphasize, 강조</em>
<u>밑줄</u>

-->

<!-- Input : 사용자가 정보를 입력하는 부분이 필요할 때 사용하는 태그  -->

	input에서도 type에 따라 어떤 형식으로 정보를 받을지 결정 가능
	<input type = "유형" 속성="속성값>
			type :
				hidden	: 사용자에게 안보임, 서버로 값을 보낼 때
				text 	: 한 줄 짜리 텍스트 입력
				search	: 검색
				password: 비밀번호 입력
				range	: 숫자를 조절할 수 있는 슬라이드 막대
				number	: 숫자를 조절할 수 있는 화살표
				checkbox: 체크박스(여러개 선택 가능)
				radio	: 라디오 버튼(1개만 선택 가능)
				image, submit, reset, button, file, ... 등등 많음  
			속성 :
				readonly : 읽기 전용
				placeholder : 힌트(음영문자) 표시(input 클릭하면 내용 사라짐)
				autofocus : 그 위치에 마우스 커서가 표시(위쪽부터 우선순위 적용됨)
				autocomplete : 자동완성
				maxlength : 최대로 입력할 수 있는 글자 수 설정

	ex)
	<input type="search" autocomplete="1111"><br>
	<input type="number" placeholder="1234"><br>
	<input type="button"><br>
	<input type="range" readonly="1111"><br>
	<input type="text" autofocus="autofocus" maxlength="8"><br>
-->
--------------------------------
<!-- field set -->
	<fieldset>
		<legend>	<!--field set 에 대한 설명을 할 때 쓰이는 태그 -->
			정보 입력
		</legend>
		<ul>
			<li>
				<label for="name">이름</label>	<!--input에 대한 설명 역할 -->
				<input id="name">
		</ul>
		<ul>
			<li>
				<label for="mail">메일주소</label>	<!--input에 대한 설명 역할 -->
				<input id="mail">
		</ul>
	</fieldset>
------------------------------------

<!--
	anchor tag
	<a> 
	: 다른 페이지나 or 같은 페이지의 특정 위치, 파일과 같은 다른 URL로 연결할 수 있는 HyperLink
	: 문법적으로 이동할 주소값을 나타내는 'href'와 함께 사용함
	href : Hyper Reference, 웹문서 or 주소값을 말함
	ex)
	<a href="https://www.naver.com">네이버</a>
-->



<!--  
			-DOM(Document Object Model) 객체-
			<태그명 속성명="값" 속성명="값"....> 시작 태그		
				텍스트 : 내용	
			</태그명> 종료 태그
			
			시작 태그만 있는 경우도 있음
			시작태그 & 종료태그만 있는 경우도 있음
				<aaa></aaa> 같이 시작태그, 종료태그 사이에 아무 내용도 없는 경우 => <aaa /> 로 축약표현 가능
				가독성 이슈로 추천은 안하는 방식
		-->
DOM : 문서 객체 모델, 즉 DOM은 웹 페이지(HTML이나 XML 문서)의 콘텐츠 및 구조, 그리고 스타일 요소를 구조화 시켜 표현하여 프로그래밍 언어가 해당 문서에 접근하여 읽고 조작할 수 있도록 API를 제공하는 일종의 인터페이스입니다. 
(https://www.codestates.com/blog/content/dom-javascript)

