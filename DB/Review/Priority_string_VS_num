대부부의 프로그래밍 언어는 형변환시 문자의 우선도가 높아서 문자 13 결과가 나오는데,
--오라클에서는 반대로 숫자의 우선순위가 더 높다.
	select 1 + 'a' from dual;
--invalid 'number' a의 숫자 형변환이 실패했음을 알려줌
--즉, 오라클에서는 연산자가 숫자면 숫자만 연산 해줌

--문자를 더해주기 위한 연산자가 따로 있음
--바로 '||' ( shift+ \ )
	select 1||'a' from dual;
